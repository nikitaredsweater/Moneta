[build]
dockerfilePath = "Dockerfile"
builder = "DOCKERFILE"

[deploy]
# No health check — this is a background worker with no HTTP port
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
